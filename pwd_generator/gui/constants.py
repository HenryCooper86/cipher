"""Shared GUI constants."""

APP_DISPLAY_NAME = "Horizon Cypher"

# Bottom CLI / shell panel (vertical splitter vs main content)
CLI_PANEL_MIN_HEIGHT_PX = 200
CLI_SPLITTER_INITIAL_TOP_PX = 580
CLI_SPLITTER_INITIAL_BOTTOM_PX = 320
# When the CLI panel is shown, allocate ~this fraction of inner height (within min bounds).
CLI_SPLITTER_CLI_FRACTION = 0.42
CLI_SPLITTER_MIN_MAIN_PX = 160
CLI_SPLITTER_MIN_CLI_PX = 220
CLI_SPLITTER_CLI_ABSOLUTE_MIN_PX = 100
CLI_SPLITTER_REBALANCE_IF_HEIGHT_BELOW_PX = 100
